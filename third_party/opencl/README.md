Khronos OpenCL C headers (https://github.com/KhronosGroup/OpenCL-Headers).
Bundled so Forge can compile the `gpu` stdlib when system opencl-headers is not installed.
Runtime still requires an OpenCL ICD (e.g. NVIDIA/AMD/Intel drivers).
