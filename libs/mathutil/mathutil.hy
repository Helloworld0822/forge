library mathutil {
    export fn twice(n: int): int {
        return n + n;
    }

    export fn add(a: int, b: int): int {
        return a + b;
    }

    export fn square(n: int): int {
        return n * n;
    }
}
