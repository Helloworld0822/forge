// Extreme benchmark: multi-threaded SO_REUSEPORT HTTP server

import http;

native main {
    let server: int = http_listen(19080);
    http_prepare(server, "Hello, World");
    http_serve_mt(server, 8);
}
